name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    # Navigate to the workspace directory to build
    - name: Navigate to Workspace
      run: cd RustPyNetWorkspace

    - name: Build
      run: cargo build --verbose

    # Navigate back to the root directory to run tests
    - name: Navigate to Root
      run: cd ..

    # Run tests from the test directory
    - name: Run tests
      run: |
        cd rustpynet_test
        cargo test --verbose
